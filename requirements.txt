# stable pypi version of numba-scipy pins scipy to an old version
# so install pre-release and pin it's dependcencies for easier resolution
git+https://github.com/numba/numba-scipy
scipy==1.7.3
numba==0.55.2
numpy==1.22.4
xarray==0.21.1

dask[array]